openapi: 3.1.3
info:
  title: Mon API
  version: 1.0.0
servers:
  - url: ' http://127.0.0.1:8000 '
paths:
  /ping:
    get:
      responses:
        '200' :
          description: ok
          content:
            text/plain:
              schema:
                type: string
                example: pong
  
